#include<iostream>
#include<algorithm>
#include<vector>
#include<bits/stdc++.h>
using namespace std;
void print(vector<vector<int> > &v){
    for(auto value: v){
        
    }
}
int main(){
    vector<vector<int> > v;
    int n;
    cin>>n;
    vector<int> arr(n);
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        arr.push_back(x);
    }
    v.push_back(arr);
    v.push_back(arr);
    cout<<"vishnu"<<endl;
}